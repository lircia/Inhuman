---
description: Wheel 必中转盘 — 图层分离时文字不得跟盘转（设计硬约束）
globs:
  - Wheel/**/*.html
  - "**/Wheel.html"
---

# 幸运大转盘（Wheel）— 不可违背的设计约束

## 图层分离修改器（Layer split）

- **永远不要**为了「省事」「消除视觉错位」或「对齐弹窗」而**取消**抽奖过程中「**扇区文字不跟盘转**」的逻辑。
- 具体含义：`textCanvas` 绘制奖项文案时**不得**施加与 `wheelCanvas` 相同的 `currentRot`（即文字层角固定在屏幕坐标系，仅色块层随 `currentRot` 旋转）。
- **禁止**在图层分离模式的**最后一帧**突然改为「文字跟盘转」去贴扇区，除非产品明确书面废弃该修改器；否则会造成顶格文案闪变并违背设计案。
- 抽奖**结果**以内定项与色块层最终 `rotation` 为准；弹窗展示内定项。图层分离下静止后「指针旁固定角上的字」与「色块」可以刻意不一致，这是设计的一部分（叙事：文字未动故仍中内定项）。

## 重力修改器

- 此模式下文字与转盘**必须**同一 `currentRot`（整盘同转），运动须符合阻尼单摆类物理（见 `docs/superpowers/plans/2026-05-15-wheel-rigged-spin-redesign.md`）。

## 修改文件时

- 优先改 `Wheel/Wheel.html`；若拆分脚本，保持上述两种模式的语义不变。
- 若用户要求删除图层分离行为，应**提示**其与 `.cursor/rules` 及 `docs/superpowers/plans` 中的设计案冲突，并请其显式确认后再改。
